package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.Command;

public class forceIndexCommand extends Command {
    /*final indexerCommand forcedIndexer;

    public forceIndexCommand(indexerCommand i) {
        forcedIndexer = i;
    }

    @Override
    public void initialize() {
        forcedIndexer.setIndexerSpeed(9);
        forcedIndexer.forceRunIndexer(forcedIndexer.indexerSpeed);
    }

    @Override
    public void execute() {}

    @Override
    public void end(boolean interrupted) {
        forcedIndexer.forceRunIndexer(0);
    }
    @Override
    public boolean isFinished() {
        return true;
    }*/
}